lsi: false
safe: true
source: src
incremental: false
highlighter: rouge
gist:
  noscript: false
kramdown:
  math_engine: mathjax
  syntax_highlighter: rouge
domain: sagetsin.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://sagetsin.github.io  # the base hostname and protocol for your site, e.g. http://example.com
